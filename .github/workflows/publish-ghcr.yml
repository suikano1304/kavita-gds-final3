name: Publish GHCR image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/publish-ghcr.yml

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/kavita-gds
  IMAGE_TAG: 0.9.0.2-1
  RELEASE_ASSET_URL: https://github.com/suikano1304/Kavita-GDS/releases/download/v0.9.0.2-1/kavita-gds.tar.gz
  RELEASE_ASSET_SHA256: 894b1b88cc1c63f886bc9413e6eda773fbf278fa5abb666fda4e632246d2177b
  OCI_ARCHIVE_PATH: kavita-gds/docker-image/kavita-gds.oci.tar

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Install skopeo
        run: |
          sudo apt-get update
          sudo apt-get install -y skopeo

      - name: Download release package
        run: |
          curl -L -o kavita-gds.tar.gz "$RELEASE_ASSET_URL"
          echo "$RELEASE_ASSET_SHA256  kavita-gds.tar.gz" | sha256sum -c -
          tar -xzf kavita-gds.tar.gz

      - name: Login to GHCR
        run: |
          echo "${{ github.token }}" | skopeo login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish version tag
        run: |
          skopeo copy --all \
            "oci-archive:${OCI_ARCHIVE_PATH}" \
            "docker://${IMAGE_NAME}:${IMAGE_TAG}"

      - name: Publish latest tag
        run: |
          skopeo copy --all \
            "oci-archive:${OCI_ARCHIVE_PATH}" \
            "docker://${IMAGE_NAME}:latest"
